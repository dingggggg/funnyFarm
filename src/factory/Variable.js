/* eslint-disable */
export default{
    install(Vue,options){
        // Vue.prototype.variable = {};
        // Vue.prototype.variable.name = 'haha';
        // Vue.prototype.variable.userMoney = 10000000;
        // if(window.localStorage && window.localStorage.farmData){
        //     var farmData = JSON.parse(window.localStorage.farmData);
        //     Vue.prototype.variable.userMoney = farmData.userInfo.money;
        //     Vue.prototype.variable.farmData = farmData;
        // }
    }
}

/* eslint-disable */
